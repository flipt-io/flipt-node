/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as environments from "../../../environments";
import * as core from "../../../core";
import { FliptApi } from "@flipt-io/flipt";
import urlJoin from "url-join";
import * as serializers from "../../../serialization";

export declare namespace Client {
  interface Options {
    environment?: environments.Environment | string;
    token?: core.Supplier<core.BearerToken>;
  }
}

export class Client {
  constructor(private readonly options: Client.Options) {}

  public async list(request?: FliptApi.FlagListRequest): Promise<FliptApi.flags.list.Response> {
    const _queryParams = new URLSearchParams();
    if (request?.limit != null) {
      _queryParams.append("limit", request?.limit.toString());
    }

    if (request?.offset != null) {
      _queryParams.append("offset", request?.offset.toString());
    }

    if (request?.pageToken != null) {
      _queryParams.append("pageToken", request?.pageToken);
    }

    const _response = await core.fetcher({
      url: urlJoin(this.options.environment ?? environments.Environment.Production, "/api/v1/flags"),
      method: "GET",
      headers: {
        Authorization: core.BearerToken.toAuthorizationHeader(await core.Supplier.get(this.options.token)),
      },
      queryParameters: _queryParams,
    });
    if (_response.ok) {
      return {
        ok: true,
        body: await serializers.flags.list.Response.parse(_response.body as serializers.flags.list.Response.Raw),
      };
    }

    return {
      ok: false,
      error: FliptApi.flags.list.Error._unknown(_response.error),
    };
  }

  public async create(request: FliptApi.FlagCreateRequest): Promise<FliptApi.flags.create.Response> {
    const _response = await core.fetcher({
      url: urlJoin(this.options.environment ?? environments.Environment.Production, "/api/v1/flags"),
      method: "POST",
      headers: {
        Authorization: core.BearerToken.toAuthorizationHeader(await core.Supplier.get(this.options.token)),
      },
      body: await serializers.flags.create.Request.json({
        key: request.key,
        name: request.name,
        description: request.description,
        enabled: request.enabled,
      }),
    });
    if (_response.ok) {
      return {
        ok: true,
        body: await serializers.flags.create.Response.parse(_response.body as serializers.flags.create.Response.Raw),
      };
    }

    return {
      ok: false,
      error: FliptApi.flags.create.Error._unknown(_response.error),
    };
  }

  public async get(key: string): Promise<FliptApi.flags.get.Response> {
    const _response = await core.fetcher({
      url: urlJoin(this.options.environment ?? environments.Environment.Production, `/api/v1/flags/${key}`),
      method: "GET",
      headers: {
        Authorization: core.BearerToken.toAuthorizationHeader(await core.Supplier.get(this.options.token)),
      },
    });
    if (_response.ok) {
      return {
        ok: true,
        body: await serializers.flags.get.Response.parse(_response.body as serializers.flags.get.Response.Raw),
      };
    }

    return {
      ok: false,
      error: FliptApi.flags.get.Error._unknown(_response.error),
    };
  }

  public async delete(key: string): Promise<FliptApi.flags.delete.Response> {
    const _response = await core.fetcher({
      url: urlJoin(this.options.environment ?? environments.Environment.Production, `/api/v1/flags/${key}`),
      method: "DELETE",
      headers: {
        Authorization: core.BearerToken.toAuthorizationHeader(await core.Supplier.get(this.options.token)),
      },
    });
    if (_response.ok) {
      return {
        ok: true,
        body: undefined,
      };
    }

    return {
      ok: false,
      error: FliptApi.flags.delete.Error._unknown(_response.error),
    };
  }

  public async update(key: string, request: FliptApi.FlagUpdateRequest): Promise<FliptApi.flags.update.Response> {
    const _response = await core.fetcher({
      url: urlJoin(this.options.environment ?? environments.Environment.Production, `/api/v1/flags/${key}`),
      method: "PUT",
      headers: {
        Authorization: core.BearerToken.toAuthorizationHeader(await core.Supplier.get(this.options.token)),
      },
      body: await serializers.flags.update.Request.json({
        name: request.name,
        description: request.description,
        enabled: request.enabled,
      }),
    });
    if (_response.ok) {
      return {
        ok: true,
        body: await serializers.flags.update.Response.parse(_response.body as serializers.flags.update.Response.Raw),
      };
    }

    return {
      ok: false,
      error: FliptApi.flags.update.Error._unknown(_response.error),
    };
  }
}
