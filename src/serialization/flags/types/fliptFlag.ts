/**
 * This file auto-generated by Fern from our API Definition.
 */

import { FliptApi } from "../../..";
import * as core from "../../../core";
import * as serializers from "../..";

export const fliptFlag: core.schemas.ObjectSchema<fliptFlag.Raw, FliptApi.flags.fliptFlag> = core.schemas.object({
  key: core.schemas.string(),
  name: core.schemas.string(),
  description: core.schemas.string(),
  enabled: core.schemas.boolean(),
  createdAt: core.schemas.date(),
  updatedAt: core.schemas.date(),
  variants: core.schemas.list(core.schemas.lazyObject(() => serializers.variants.fliptVariant)),
});

export declare namespace fliptFlag {
  interface Raw {
    key: string;
    name: string;
    description: string;
    enabled: boolean;
    createdAt: string;
    updatedAt: string;
    variants: serializers.variants.fliptVariant.Raw[];
  }
}
