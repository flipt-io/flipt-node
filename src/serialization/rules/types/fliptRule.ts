/**
 * This file auto-generated by Fern from our API Definition.
 */

import { FliptApi } from "../../..";
import * as core from "../../../core";
import * as serializers from "../..";

export const fliptRule: core.schemas.ObjectSchema<fliptRule.Raw, FliptApi.rules.fliptRule> = core.schemas.object({
  id: core.schemas.string(),
  flagKey: core.schemas.string(),
  segmentKey: core.schemas.string(),
  distributions: core.schemas.list(core.schemas.lazyObject(() => serializers.distributions.fliptDistribution)),
  rank: core.schemas.number(),
  createdAt: core.schemas.date(),
  updatedAt: core.schemas.date(),
});

export declare namespace fliptRule {
  interface Raw {
    id: string;
    flagKey: string;
    segmentKey: string;
    distributions: serializers.distributions.fliptDistribution.Raw[];
    rank: number;
    createdAt: string;
    updatedAt: string;
  }
}
